import 'package:flutter/material.dart';

const secondColor = Color(0xFFF2F2F0);
const textColor = Colors.white;

const defaultPadding = 20.0;

const defaultShadow = BoxShadow(
  offset: Offset(0, 6),
  blurRadius: 8,
  color: Colors.white10,
);